[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "quant-core"
version = "0.1.0"
requires-python = ">=3.12"

[tool.maturin]
features = ["python"]
module-name = "quant_core"
